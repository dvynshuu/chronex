{
  "name": "chronex-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build:client": "npm install --prefix client && npm run build --prefix client"
  }
}
